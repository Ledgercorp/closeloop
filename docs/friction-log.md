# Friction Log

Capture real friction only. Do not manufacture issues.

| Date | Amazon tool/service | Task attempted | Expected | Actual | Severity | Workaround | Actionable recommendation |
|---|---|---|---|---|---|---|---|
| 2026-09-05 | N/A | Initial local project scaffold | Repo can be created locally | Completed | N/A | N/A | N/A |
| 2026-09-05 | Alexa+ MCP Toolkit / Alexa AI CLI | Install the official CLI and Add-on Agent Skill | Approved builders can install `@alexa-ai/cli` and the official skills | The host has Node 22 (24+ required), no AWS CLI/profile or CodeArtifact registry configuration, and the package returns npm 404 from the public registry; the developer console requires sign-in | Blocking for live onboarding | Continue with protocol-level compatibility and official MCP SDK inspection; do not claim Alexa+ authentication or deployment | Provide a public bootstrap that detects partner entitlement and installs Node, AWS tooling, CLI, inspector, and agent skills without requiring a Solutions Architect handoff |
| 2026-09-05 | Alexa+ MCP Toolkit / deployment | Reach the existing self-hosted CloseLoop MCP URL | Repository homepage should resolve to a public HTTPS MCP endpoint | `closeloop-sage.vercel.app` returns `DEPLOYMENT_NOT_FOUND`; the successful deployment URL reported by GitHub redirects to Vercel SSO | Blocking for Alexa+ registration | Validate locally and require a new public deployment before onboarding | Add CLI preflight that distinguishes an application 401 from hosting-layer login or a missing deployment |
| 2026-09-05 | Alexa+ authentication docs | Reconcile discovery behavior with the MCP SDK | SDK defaults should satisfy the documented Alexa+ discovery contract | MCP SDK 2.1.1 returns a `WWW-Authenticate` header and path-suffixed PRM URL, while Alexa+ QuickStart requires a 401 without that header and documents the root PRM path | Medium | Add an Alexa-compatible root PRM alias and strip only the unsupported challenge header on `/mcp` 401 responses | Publish a tested Python reference configuration for Alexa+ authentication and clarify the path-suffixed RFC 9728 form |
| 2026-09-05 | MCP Inspector 2.5.0 | Run the strongest public official MCP validation available | Inspector should run on the existing Node 22 host | Inspector requires Node `>=22.19.0`, while the host is `22.14.0`; it emitted an engine warning but its strict CLI operations completed successfully | Low for local inspection; unsupported runtime | Record the warning and rely on deterministic integration tests plus successful CLI exits; upgrade Node before further official tooling | Align the Alexa and MCP Inspector setup docs around one preflight-supported Node version |
| 2026-09-05 | AWS SDK / DynamoDB | Exercise the AWS-backed repository and CloudFormation deployment | Validate against a low-cost live AWS table or official DynamoDB Local | No AWS CLI, profile, or `AWS_*` credentials are present; DynamoDB Local 2.6+ requires Java 17 or Docker, and this host has neither a usable JRE nor Docker | Blocking for live/AWS-native verification | Run the boto3 integration against Moto, inspect every request contract, and label the result SIMULATED | Offer a credential-free official DynamoDB emulator binary/container path and a single preflight command that distinguishes live, local, and simulated validation |
| 2026-09-05 | DynamoDB Query/data modeling | Preserve the existing created-time ordering for owner-scoped open items | Query owner records and apply the API limit directly | The required sort key is `resolution_id`; DynamoDB applies `Limit` before a filter, and a GSI cannot provide strongly consistent reads | Medium design friction | Paginate the owner partition to exhaustion, remove terminal items, sort by `created_at`, then cap; document owner-history read cost | Documentation examples should prominently explain API-limit/filter ordering and consistency tradeoffs together |
| 2026-09-05 | DynamoDB conditional writes | Preserve precise CloseLoop domain errors after one atomic transition attempt | Conditional failure should safely distinguish terminal, stale, missing, and illegal writes | `ConditionalCheckFailedException` does not identify which clause failed without an additional option/read | Low | Perform one strongly consistent diagnostic read only after failure; never use it to retry or authorize a write | Provide clearer SDK examples for condition-failure classification that preserve atomic authorization boundaries |
| 2026-09-06 | MCP Apps `AppBridge` validation host | Embed the exact production MCP App HTML in an outer module-script validation host | JSON serialization should safely preserve the nested app document | `json.dumps` escaped JavaScript string syntax but left literal `</script>` text, so the HTML parser ended the outer script before the bridge connected | Medium | Serialize every script-embedded value with ASCII JSON and replace `<` with `\u003c`; retain an injection regression and connect the bridge before assigning `srcdoc` | Provide an official script-context-safe host helper or complete Python-host example for validating `srcdoc` MCP Apps |
| 2026-09-06 | Alexa+ MCP Apps rendering | Validate the polished proof card in an actual Alexa+ host or Local Inspector | Confirm host-specific layout, semantics, and interaction after local MCP Apps validation | Selected-partner Alexa tooling and Local Inspector remain unavailable on this host, so rendering proof is limited to the official `AppBridge` / `PostMessageTransport` path in a local browser | Blocking for Alexa+-native visual proof | Verify all real lifecycle outcomes, keyboard disclosure, accessibility tree, and narrow viewport locally; label the result LOCAL UI/BROWSER VERIFIED rather than Alexa+ verified | Offer a public, credential-free MCP Apps renderer or entitlement preflight so add-on teams can separate UI defects from partner-access blockers |
| 2026-09-06 | MCP input validation | Reject non-boolean confirmation payloads at the public schema boundary | JSON strings and numbers that look truthy should be rejected | Pydantic's default boolean conversion accepted values such as `"true"`, `"yes"`, and `1`, allowing execution | Blocking security finding | Use `StrictBool` and retain end-to-end MCP regression cases for coercible values | Security-sensitive MCP examples should default to strict scalar types and call out coercion explicitly |
| 2026-09-06 | DynamoDB conditional-write concurrency | Classify the losing side of simultaneous confirmations predictably | The loser should observe a stale-version or invalid-transition error | A post-failure diagnostic read can legitimately observe that the winner already terminalized the row, yielding terminal-outcome immutability instead | Low; safe behavior | Accept all three fail-closed domain classifications while asserting a single provider execution and a single terminal outcome | Document that diagnostic error classification is observation-time dependent even though the conditional write guarantee is deterministic |
| 2026-09-06 | MCP Apps evidence trust boundary | Prevent evidence-shaped payloads from manufacturing a visual Verified state | Outer lifecycle/result consistency checks should fail closed | A structurally plausible payload with forged provenance, reordered history, or contradictory read-back could satisfy the original outer tuple check | Blocking security finding | Re-evaluate the deterministic evidence predicate in the card and require canonical provenance and lifecycle history | MCP Apps guidance should include threat models for untrusted or compromised host result payloads, not only rendering and transport examples |
| 2026-09-06 | Confirmation authorization design | Bind a confirmation to the exact user-approved action | Owner and resolution authorization should prove the specific utterance approved the selected pending action | The original `{resolution_id, confirmed}` shape had no trusted action binding, issue time, or expiry; a nonce returned to the same compromised agent would not prove user approval | Blocking finding reproduced; resource-server gap closed locally | Require a signed, short-lived, single-use v1 attestation from a separately trusted confirmation authority; deny all confirmations without complete production configuration | Alexa+ add-on guidance should provide a standard short-lived, replay-resistant confirmation-attestation claim set and host delivery mechanism for consequential MCP tools |
| 2026-09-06 | Alexa+ account linking / confirmation | Connect authenticated write requests to exact per-action human approval | Official account-linking guidance should define a host-signed confirmation artifact usable by an MCP resource server | The documentation defines OAuth 2.1 user tokens and write-operation authentication but no per-action confirmation attestation | Blocks live end-to-end confirmation proof, not local resource-server verification | Add a deny-by-default verifier interface and require an external trusted issuer to sign the exact owner/resolution/action digest; use only a labeled test issuer locally | Publish a standard short-lived, replay-resistant confirmation-attestation claim set and host delivery mechanism for consequential MCP tools |
